[project]
name = "cafeflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shashi khadka", email = "shashikhadka84@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "django>=6.1",
    "pillow>=12.3.0",
    "requests>=2.34.2",
]

[project.scripts]
cafeflow = "cafeflow:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
